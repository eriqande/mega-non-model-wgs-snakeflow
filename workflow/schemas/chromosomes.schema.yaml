$schema: "http://json-schema.org/draft-04/schema#"

description: an entry in the chromosomes sheet
properties:
  chrom:
    type: string
    description: chromosome name
  num_bases:
    type: integer
    description: length of the chromosome in number of bases
required:
  - chrom
  - num_bases
